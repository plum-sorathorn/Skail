# Rudder Evaluation & Routing Gates

Status: Release sign-off withheld
Date: 2026-09-04
Tracker: [v0.1.0 release remediation](../../tasks/rudder-v0.1.0-release-remediation.md)

The previous report is invalid for release sign-off. Its scripted execution was coupled to
oracle expectations, its timing fixtures did not demonstrate equivalent useful parallel work, and
the reported speedup is not supported by the preserved diagnostics. It must not be cited as a
provider-quality result, provider-cost saving, safety guarantee, or release verification.

Rudder retains offline fixtures for contract testing. They exercise the runtime under deterministic
fake responses; they do not establish real-provider quality or savings. Live-provider validation
is opt-in and has not been performed for v0.1.0.

## Current methodology record

- The documented end-to-end parallel gate remains at 15% against the serial policy.
- With the same 450 ms synthetic child delay, the preserved diagnostics reported 23.4% for seed
  42 and 14.5% for seed 100. The latter fails the gate.
- An interrupted change raised the delay to 650 ms. It was removed without using it as release
  evidence; increasing simulated latency is not a valid performance remedy.
- Existing files in `evals/results/` are retained only as diagnostics and are invalid for release
  sign-off. New release evidence will be generated by the strengthened evaluator from an isolated
  candidate checkout and will retain raw repetitions, environment metadata, and failed runs.

## Required evidence before release

The evaluator and release checker must independently validate fixture/oracle separation, actual
provider usage fixtures, equivalent serial and parallel workloads, persisted assignments and
settlements, overlap, raw wall-clock measurements, and recomputed gates. The complete requirements
and phase acceptance evidence are maintained in the remediation tracker.
